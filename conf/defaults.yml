server:
  # Host and port to which server will bind to.
  # Use 0.0.0.0 to bind to all hosts/ips for the machine
  host: "0.0.0.0"
  port: 8080

winston:
  console:
    enabled : true

# Auto injected properties
DISCOVER_NODE_NAME: 'local-1'
GIT_COMMIT: 'NA'