enabled: true

variables:
  stop_timeout: "30s"
  enable_travis: true
  enable_public_host: true
  node_count: 2
  min_nodes: 1
  public_host: 'totem.meltdemo.com'
  force_ssl: true
  
deployers:
  __defaults__:
    templates:
      app:
        args:
          environment:
            TEST_ENV: 
              value: "Test quotes $var ' \" ` "
            TEST_ENV2: "Test val"
