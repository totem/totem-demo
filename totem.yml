enabled: true

variables:
  stop_timeout: "30s"
  enable_travis: true
  enable_public_host: true
  node_count: 2
  min_nodes: 1
  public_host: 'totem.meltdemo.com'
  
deployers:
  __defaults__:
    templates:
      app:
        args:
          environment:
            TEST_ENV: 
              value: "Test quotes ' \" "
